import { JobDetails } from "../../../components/JobDetails";

export default function JobsDetailsPage() {
  // the main is in the page that has the data from the file, then try to pass it to the other files
  return <JobDetails />;
}
